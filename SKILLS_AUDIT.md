# 스킬 목록 진단 (2026-08-09)

계정에 활성화된 Claude 스킬 7종을 점검한 결과와 조치 내역. 스킬 본문은 트리거될 때만 로드되므로, 안 쓰는 스킬의 비용은 매 세션에 실리는 설명 텍스트와 오트리거 가능성이다. 계정 스킬의 활성/비활성은 claude.ai **설정 → 기능(스킬)** 에서만 바꿀 수 있다(세션에서 대신 삭제 불가).

## 판정 요약

| 스킬 | 출처 | 판정 | 근거 |
|---|---|---|---|
| gyeseong-korean-qa | 직접 제작 | **korean-naesin-qa로 대체** | 계성고 전용이던 검수를 학교 무관 공통 기준으로 일반화(아래 참조). 새 스킬 설치 후 이 스킬은 삭제 |
| docx / pdf / xlsx | Anthropic 공식 | 유지 | 수업 자료·문서 작업에 실사용 가치. 트리거 조건이 명확해 충돌 없음 |
| pptx | Anthropic 공식 | 유지 (조건부) | 슬라이드를 만들 일이 없으면 비활성화해도 무방 |
| morning | Anthropic 예제 | **비활성화 권장** | `/morning` 브리프를 명시적으로 부를 때만 동작. 캘린더·메일 연동 전제 기능이라 안 쓰면 목록 소음일 뿐 |
| skill-creator | Anthropic 예제 | 유지 (선택) | 스킬을 만들거나 고칠 때만 필요. 당분간 스킬 작업이 없으면 꺼 두었다가 필요할 때 켜면 된다 |

스킬 간 트리거 중복·충돌은 없다. docx와 gyeseong-korean-qa가 같은 문서에서 함께 뜰 수 있으나 역할이 다르다(파일 조작 vs 내용 검수).

## 신규 스킬 2종 — 세션 78개 분석 기반 (같은 날 후속)

지난 세션의 반복 작업 패턴에서 추출해 새로 만든 스킬. 규칙은 academy-manager 코드베이스의 실제 출제 엔진·리포트 엔진에서 뽑았다(앱과 손작업이 같은 기준을 쓰도록).

| 스킬 | 내용 | 출처 |
|---|---|---|
| **korean-exam-writer** | 국어 문제 출제 기준 — 5지선다 기본형, 매력적 오답 금지 철학, 정형 발문 화이트리스트, 오답 설계 6종+10패턴, 보기 작성 5종, 해설 "정답 : ③"+통합 서술, 표기 규약·금지 표현, 풀세트 구성(문학 5·7/비문학 5/문법 3), 난이도 5단계 | koreanQuestionPrompts.js·koreanQuestionExplanation.js·koreanQuestionLocalRules.js·questionGen.js |
| **student-report** | 학생 분석·보고서 작성 기준 — 데이터 기반 9원칙(구체 수치 근거·관통 스토리·밀도·반복 금지·처방은 빈도/시간/분량), 청자 4종(학부모/학생/강사/원장) 톤·안전선, 보고서 유형 4종 형식(종합 보고서·질문 분석 진단·주간 진단·운영 브리핑), 금지 목록(타 학생 비교·진단명·AI 표기 등) | reports.js(r5 프롬프트)·weeklyStudentDiagnosis.js·weeklyReportGenerator.js·questions.js 진단·tone-guide |

주의: `<보기>` 인물 이름용 실제 학생 명단은 개인정보라 스킬에 넣지 않았다(공개 저장소). 앱 출제 설정의 화이트리스트를 참조하도록 지침만 두었다.

## korean-naesin-qa — 학교 무관 일반화 (같은 날 후속)

계성고 전용 스킬을 대체하는 새 스킬. `skills/korean-naesin-qa/`에 보관한다(버전 관리용).

- **구조**: 검수 로직·문어체 톤 규칙은 학교 무관 공통 기준으로 SKILL.md·검수.py에, 학교별 특징(작품 배정표·허용 예외·출처 주의·판별 마커)은 `references/<학교>.md`로 분리. 계성고가 첫 등록 학교이고, `references/_학교_양식.md`로 새 학교를 추가한다.
- **검수.py 확장**: 학교 자동 판별(마커 기반, 등록 학교가 하나면 폴백) + `--학교 이름` 수동 지정. 학년 구분이 2개로 고정돼 있던 것을 학년 수 제한 없는 구조로 변경(고3 추가 가능). 혼입 경고에 원소속 학년 표기.
- **데이터 동기화 규칙**: 배정표는 `references/<학교>.md`와 `검수.py`의 `SCHOOLS` 두 곳에 있으므로 항상 함께 수정한다(SKILL.md에 명시).
- 아래 '최적화 내역'의 수정 사항(YAML·죽은 코드·종료 코드 등)은 모두 승계했다.

## (구) gyeseong-korean-qa 최적화 내역

1. **SKILL.md frontmatter YAML 오류 수정** — description이 따옴표 없이 `예:` 등 콜론을 포함해 표준 YAML 파서에서 파싱 실패(패키징 검증 실패 원인). 블록 스칼라(`>-`)로 교체, 설명 문구 자체는 동일.
2. **죽은 코드 삭제** (`검수.py`) — 아무 동작도 하지 않는 이중 공백 검사 블록(`if "  " in ...: pass`) 제거.
3. **중복 검사 제거** — 톤 검사에서 `TONE` 리스트 매칭 후 같은 패턴을 다시 확인하는 `re.search` 중복 호출 삭제. 이에 따라 쓰이지 않게 된 `re` 임포트도 제거.
4. **파일 없음 처리 수정** — 검사할 파일이 없으면 "검사할 파일 없음"과 함께 "종합: 검토 필요 ⚠"가 출력되던 오동작을 정리(이제 조용히 종료).
5. **종료 코드 추가** — 통과 시 0, 문제 발견 시 1을 반환해 재검수 자동화(스크립트 연계)가 가능.
6. **주석 1줄 추가** — '길'·'회고가'·'십자가' 같은 짧은 제목이 혼입 목록에서 빠진 이유(오탐 위험)를 명시.

검증: 학년 혼입 검출, 톤 후보 검출, 높임 예시 화이트리스트, 어부사시사 허용 예외, 종료 코드 모두 테스트 통과.

## 계정에 반영하는 법

1. 세션에서 전달한 `.skill` 파일 3종(`korean-naesin-qa` · `korean-exam-writer` · `student-report`)을 각각 **저장(Save skill)** 버튼으로 설치한다.
2. 설정 → 기능(스킬)에서 기존 `gyeseong-korean-qa`를 삭제한다(korean-naesin-qa가 완전히 대체).
3. morning 등 비활성화는 같은 화면에서 토글한다.
